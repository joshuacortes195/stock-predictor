# Runtime-only dependencies for the deployed serving image (no notebooks,
# plotting, or test tooling). Versions must stay in lockstep with
# requirements.txt — that file is the source of truth.
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0
joblib==1.5.3
yfinance==1.5.1
lxml==6.1.1
flask==3.1.3
flask-cors==6.0.5
gunicorn==26.0.0
psycopg[binary]==3.3.4
