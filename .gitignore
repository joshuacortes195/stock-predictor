# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/

# Data (downloaded via scripts/download_data.py, not committed)
data/*
!data/.gitkeep

# Trained model artifacts (regenerated by training scripts) — the small
# final demo model + its metrics ARE committed so the API works out of the box
models/*
!models/.gitkeep
!models/direction_model.joblib
!models/metrics.json

# Secrets / local config
.env
kaggle.json

# Frontend (added in Phase 5)
node_modules/
dist/

# OS / editor
.DS_Store

# Claude Code local settings (machine-specific, not shared)
.claude/
